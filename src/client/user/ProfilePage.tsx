import { createEffect, createSignal, JSX, Show } from 'solid-js'
import { Button, H1, LabelItem } from '../components'

export default function ProfilePage(): JSX.Element {
  return <span>temp</span>
}
